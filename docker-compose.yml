services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: fastforge
      POSTGRES_PASSWORD: fastforge
      POSTGRES_DB: fastforge
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: